/**
 * Simple build script that compiles TypeScript to JavaScript
 * for use in ESLint config
 */

// Simple approach: just copy the index and add .js extension handling
// In a real setup, you'd use tsup or esbuild
// The plugin will be loaded as-is by ESLint with TypeScript support
// via @nuxt/eslint's TypeScript handling
