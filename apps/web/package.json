{
  "name": "where-run",
  "type": "module",
  "private": true,
  "version": "1.17.2",
  "scripts": {
    "build": "nuxt build",
    "db:ready": "pnpm run db:migrate && pnpm run db:seed",
    "dev": "pnpm run db:ready && (doppler run -- nuxt dev || nuxt dev)",
    "quality": "echo 'Turbo dependsOn handles lint + typecheck + format:check'",
    "db:migrate": "bash ../../tools/db-migrate.sh where-run-db --local --dir drizzle",
    "db:seed": "wrangler d1 execute where-run-db --local --file=node_modules/@narduk-enterprises/narduk-nuxt-template-layer/drizzle/seed.sql",
    "db:reset": "bash ../../tools/db-migrate.sh where-run-db --local --dir drizzle --reset && pnpm run db:seed",
    "db:studio": "drizzle-kit studio",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "typecheck": "nuxt typecheck",
    "deploy": "nuxt build && wrangler deploy",
    "prelint": "test -f .nuxt/eslint.config.mjs || nuxt prepare",
    "lint": "eslint . --max-warnings 0",
    "test:e2e": "playwright test --project=web --config=../../playwright.config.ts",
    "format:check": "prettier --check \"**/*.{ts,mts,vue,js,mjs,json,yaml,yml,css,md}\" --ignore-path ../../.gitignore"
  },
  "dependencies": {
    "@narduk-enterprises/narduk-nuxt-template-layer": "workspace:*",
    "@narduk-enterprises/eslint-config": "^1.0.12",
    "drizzle-orm": "^0.45.1",
    "nuxt": "^4.3.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "drizzle-kit": "^0.31.9",
    "eslint": "^9.39.4",
    "nitro-cloudflare-dev": "^0.2.2",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "vue-tsc": "^3.2.5",
    "wrangler": "^4.20.0",
    "@cloudflare/workers-types": "^4.20250303.0",
    "@iconify-json/lucide": "^1.2.94"
  },
  "overrides": {
    "@cloudflare/unenv-preset": "2.12.1",
    "vite": "npm:rolldown-vite@7.3.1"
  },
  "volta": {
    "node": "22.22.0"
  }
}
